apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservice-pro-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: microservice-pro.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microservice-pro-service
            port:
              number: 80
